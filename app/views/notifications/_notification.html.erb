
<% visiter = notification.visiter %>
<% recruitment = notification.recruitment %>

<div class="my-3 p-3 bg-white rounded shadow-sm">
  <%= link_to users_path(notification.visiter) do %>
    <%= attachment_image_tag visiter, :profile_image, :fill,100,100, format: "jpeg", fallback: "no_image.jpg", size: "50x50", class: "rounded-circle" %>
  <% end %>
  <%= notification_form(notification) %><span><%= " (#{time_ago_in_words(notification.created_at)} 前)" %></span>
</div>

